# Core
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
requests>=2.31
pyarrow>=14.0        # Parquet caching

# Models
xgboost>=2.0
tensorflow>=2.13     # Optional — falls back to sklearn if absent

# Hyperparameter tuning
optuna>=3.3          # Optional — skipped if absent

# Interpretability
shap>=0.44           # Optional — skipped if absent

# Visualisation
matplotlib>=3.7

# Dev / notebook
jupyter>=1.0
ipykernel>=6.0
